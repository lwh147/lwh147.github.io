自己的基础框架开发
迭代商品仓库管理项目
学习Mysql优化问题
学习并发编程

mysql索引细则
mysql隐式转换
jackson全局配置日期时间格式化格式后使用字段注解JsonField无效
接口文档生成工具，swagger、smart-doc（无侵入）、knife
打印匹配的前后n行内容：grep -n 正则
使用map接收参数时，value类型为Object而不是String
数据库需要枚举的字段可以直接使用java枚举类型代替String类型，枚举值加@EnumValue注解即可（使用mybatis-plus框架）

pid=`ps -auxww | grep dvh-resource-service-1.0.0-SNAPSHOT.jar | grep -v grep | awk '{print $2}'`
if [ -n "$pid" ] ;then
   kill -9 $pid
   sleep 5
   echo  "关闭 $pid"
fi
nohup java -jar dvh-resource-service-1.0.0-SNAPSHOT.jar  > /dev/null 2>&1 &
echo "dvh-resource-service服务启动"